{
  "compilerOptions": {
    "noImplicitAny": true, // For�a a declarar tipos, evitando o 'any' impl�cito.
    "noEmitOnError": true, // N�o gera o arquivo .js se houver erros no .ts.
    "removeComments": false, // Mant�m os coment�rios no .js final.
    "sourceMap": true,       // Essencial! Cria um arquivo .map para debugging do .ts no navegador.
    "target": "es6",         // Define a vers�o do JavaScript que ser� gerada (es6 � um bom padr�o).
    "outDir": "wwwroot/js"   // MUITO IMPORTANTE: Onde os arquivos .js compilados ser�o salvos.
  },
  "include": [
    "ClientApp/**/*.ts"      // MUITO IMPORTANTE: Onde est�o seus arquivos-fonte .ts.
  ],
  "exclude": [ "node_modules" ]
}